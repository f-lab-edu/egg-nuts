package me.eggnuts.usermanager.interfaces.message;

public enum ErrorMessage implements MessageCommon{
    NO_GENDER,
    NO_STATUS,
    UNSUPPORTED_TYPE,
    UNKNOWN,
    UNKNOWN_USER,
    DUPLICATE_ID,
    ID_EMPTY;
}
